import { Layout } from "@/src/components";

export default function Home() {
    return (
        <Layout>
            <main>
                <section className="container">
                    <h1>Hello, world!</h1>
                    <p>It's working.</p>
                </section>
            </main>
        </Layout>
    );
}
